"""Тесты nanoservices на stdlib unittest.

Запуск из корня репозитория (нужен импорт пакета — см. bootstrap в файлах):

	python3 -m unittest discover -s tests -v

Либо из родителя репозитория:

	python3 -m unittest discover -s nanoservices/tests -t . -v
"""
