Напиши обратный http proxi на asyncio
Backend обращается к заданному URL 
К фронтенду чтобы могли одновременно подключаться несколько клиентов
Нужна поддержка streaming http и обработка ошибок сети

чтобы можно было добавлять ещё классы типа StreamLogger 
чтобы как бы конвейер мог получиться

Пусть у нас каждый класс представляет сервер. Сервер представляет фичу "вопрос-ответ". 
Но ответ может прийти асинхронно. Причем возможна ситуация, когда клиент начал стримить запрос, а сервер прямо сразу начинает стримить ответ.       
Возможна ситуация, когда у нас не линейный пайплайн а у сервера имеется несколько бэкэндов и он роутит запросы в разные бэкэнды.             Я думаю надо в методе request_headers() передавать не только http метод, путь, заголовки, инфу о клиенте, но и функции (возвращающие корутины) response_start, response_chunk, response_end.                        Таким образом у нас http сервер будет инициализировать логгером, а логгер будет инициализировать сендером на определенный адрес.                                        И в дальнейшем можно будет добавить StdioSender, StdioServer (обычно у сервера клиентов может быть много, но в данном случае только один) , Эти Stdio* могут быть разными потому что надо договориться, как отделяются сообщения друг от друга.                                В дальнейшем можно будет добавить роутер с каким нибудь кастомным поведением... Ну и другие преобразователи

Ещё я заметил, я частенько забываю писать await. Давай договоримся async методы начинать с буквы a. А async генераторы начинать с af.                        

Теперь я думаю надо сделать так:
class Handler:
    async def ahandle(
        self,
        data: Any,
        client_addr: Any,
        afread_request: afReadChunk,
        aresponse_start: aResponseStart
    ) -> None:
        async for chunk in afreaf_request():
           ...
        await aresponse_start (request_headers, self.afread_respond)
    async def afread_respond(self):
        while has_data:
            yield data

как раз через async for можно будет и исключения кидать...

Моя архитектура симметрична: источник данных является генератором, а потребитель делает async for
Для параллельного стриминга нужна отдельная задача - тут ты прав. Это будет выглядеть так:
async def ahandle(self, ..., afread_request, aresponse_start):
    # analyze header
    pump_task = asyncio.create_task(aresponse_start(headers, self.afread_respond))
    async for chunk in afread_request():
        ...
    await pump_task # дожидаемся что все части ответа отправлены

но в нашей задаче в большинстве случаев стриминга в запросе не будет, по этому можно не создавать новые задачи а слать ответ после получения всего запроса (как в моём предыдущем примере)
ошибки:
если ошибка в источнике: генератор кидает исключение и async for его ловит
если ошибка в приёмнике: async for кидает исключение и генератор ловит GeneratorExit

напиши обёртку в нашем стиле для
            backend_reader, backend_writer = asyncio.open_connection(self.target_host, self.target_port, ssl=ssl_ctx)
с поддержкой стриминга только в ответе (http-чанки + если обнаружил заголовки ServerStreamEvents, то парсинг этих частей (там формат `data:` и в конце `data: [DONE]`))
с поддержкой keep-alive

и обычный http сервер, который это использует

я ожидал что класс HttpBackend будет как сервер: когда создаёшь его экземпляр это как буд то я поднимаю сервер
а keep-alive он инкапсулирует внутри и если надо переподнимает соединение незаметно для пользователя

Фронтенд-сервер (приём запросов от клиентов) — реализуем свой парсер HTTP/1.1 на asyncio streams (в общем стиле проекта) или используем готовую либу (aiohttp) только для приёма, а кастомная логика — с backend?
Свой парсер HTTP/1.1 (всё кастомное)
Keep-alive к backend: одно TCP-соединение, переиспользуемое последовательно (запрос за запросом), или пул из нескольких соединений для параллельных клиентов?
Мы пишем сервер. У него одновременно может быть несколько клиентов. На каждого клиента одно keep-alive соединение
Как HttpBackend встраивается в конвейер: сам реализует Handler.ahandle (то есть это обычный элемент пайплайна, который форвардит запрос в backend), или это отдельный вспомогательный класс с другим API, который вызывается изнутри вашего Handler'а?
HttpBackend сам реализует ahandle (Handler)

-----------------------------------------------------------

пара недочётов
`ARequestInfo, ARespondInfo, _ABackendConnection, _areason_phrase` - это не awaitable объекты/типы, здесь буква A/a в начале не нужна
SSE: afread_sse_events: из каждого сообщения надо убрать `data:` и `data: [DONE]` не надо пересылать
HttpServer: должен анализировать заголовки ответа, и если там есть Content-Type:text/event-stream, то надо добавлять `data:` к сообщениям и data: `[DONE]` в конце

И может быть client_addr стоит засунуть в RequestInfo, но это на твоё усмотрение

Я тут подумал, формат

async def ahandle(self, ..., afread_request, aresponse_start):
    # analyze header
    pump_task = asyncio.create_task(aresponse_start(headers, self.afread_respond))
    async for chunk in afread_request():
        ...
    await pump_task # дожидаемся что все части ответа отправлены

давай его назовём ahandle_duplex и будем его использовать если возникнет необходимость в стриминге запроса

а пока будем использовать более простой формат

async def ahandle(self, req_head, afreq_gen): # или можно прямо даные req_body:bytes вместо afreq_gen
        ...
    return resp_head, afresp_gen

и использовать это

resp_head, afresp_gen = await server.ahandle(req_head, req_body)
отсылаем resp_head
async for chunk in afresp_gen():
    отсылаем chunk

и соответственно всё перепишем


Одно место, на которое стоит обратить внимание: 
возвращённый afread_response держит за собой лок и соединение с backend, поэтому его обязательно надо проитерировать до конца или закрыть 
— HttpServer делает это через finally: await body_gen.aclose(). 
Освобождение ресурсов сидит в finally генератора, и оно строго синхронное: 
await на пути GeneratorExit запрещён, поэтому фактическое закрытие сокета уводится в asyncio.create_task. 
Проверил отдельным тестом — при резком обрыве клиента посреди стрима соединение к backend выбрасывается, лок освобождается, сервер продолжает работать.

-----------------------------------------------------------

Давай теперь напишем StreamLogger

в папке log на каждый запрос клиента создаётся файл с именами time-host-port.txt
host-port - клиента, time - текущее время запроса. 
разделители и сообщения в файле будут в формате
==== случайная_строка_10_байт сообщение ====

формат внутри такой:

пустое сообщение
method, path, http_version как в http
заголовки запроса
пустая строка
тело запроса
сообщение RESPOND
method, path, http_version как в http
заголовки ответа
пустая строка
тело ответа

При логгировании стриминга если время между приходом предыдущего кусочка и приходом текущего кусочка меньше 1сек - просто дописывай кусочек в файл на новой строке, 
а если больше 1 сек - вставляй в файл сообщение, содержащее время 
    между приходом предыдущего кусочка и приходом текущего кусочка
    между приходом первого кусочка и приходом текущего кусочка
на одной строке

плюс добавь ротирование: если в папке log больше 100 .txt файлов - удаляй самый старый .txt файл

log и 100 - параметры конструктора

если есть заголовок запроса content-type: application/json, то выводим тело запроса со спец форматом
если есть заголовок ответа content-type: application/json, или заголовок запроса accept: application/json то 
каждый чанк ответа выводим со спец форматом

спец формат:
если данные короче 200 символов
    выводим без формата как есть
если данные короче 500 символов
    данные нужно прочитать в json и вывести в форматированном json как-нибудь по убористей, чтобы меньше строк они заняли и длина строк не превысила 200 символов
иначе
    записать в лог сообщение "YAML" и потом прочитать данные в json и вывести в формате yaml,
при чём строки надо выводить через | с сохранением начальных  и хвостовых пробелов: например для данных (если бы их общая длина была бы больше 500 символов)
```
data = {
    "f": "asdf   \n    sdfg    \n",
    "g": "asdf   \n    sdfg    \n"
}
```
в логе должно появиться
```
f: |
  asdf   
      sdfg    
g: |
  asdf   
      sdfg    
```

в консоль при запросе и при завершении ответа (успешном или не очень) вывести соответствующее сообщение, содержащее time-host-port

порядок аргументов в конструкторах сделать так 
HttpBackend(url)
StreamLogger(backend, dir, max_files)
HttpServer(pipeline, host="0.0.0.0", port=8000)

И в example_proxy.py добавить парсинг аргументов: url, dir, host, port
